/*
run the lines below in Scala shell
 */
class GreetingSimple {
  val greeting = "Hello there"

  def greet() = {
    println(greeting)
  }
}

/*
run the lines below in Scala shell
val g = new GreetingSimple ()
g.greet()
run the lines below in Scala shell
 */

